package com.spboot.DohaRiff.repository;

import com.spboot.DohaRiff.entity.DohaEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DohaRepository extends JpaRepository<DohaEntity,Long>{
}
